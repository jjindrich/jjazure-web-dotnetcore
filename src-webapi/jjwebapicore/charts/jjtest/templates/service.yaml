apiVersion: v1
kind: Service
metadata:
  name: {{ include "jjtest.fullname" . }}
  labels:
    {{- include "jjtest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jjtest.selectorLabels" . | nindent 4 }}
