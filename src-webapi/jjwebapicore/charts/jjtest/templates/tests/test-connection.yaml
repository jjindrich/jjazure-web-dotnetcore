apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jjtest.fullname" . }}-test-connection"
  labels:
    {{- include "jjtest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jjtest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
