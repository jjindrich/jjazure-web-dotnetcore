apiVersion: v1
kind: Service
metadata:
  name: {{ include "jjwebcore.fullname" . }}
  labels:
    {{- include "jjwebcore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jjwebcore.selectorLabels" . | nindent 4 }}
