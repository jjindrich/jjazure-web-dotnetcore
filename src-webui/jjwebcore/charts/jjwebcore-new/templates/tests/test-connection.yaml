apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jjwebcore.fullname" . }}-test-connection"
  labels:
    {{- include "jjwebcore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jjwebcore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
