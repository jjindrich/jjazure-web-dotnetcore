name: Test

on:
  workflow_dispatch:

jobs:

  test:
    name: 'Bake'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src-webapi

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - run: |
        ls ./jjwebapicore/charts/jjtest

    - uses: azure/k8s-bake@v1
      with:
        renderEngine: 'helm'
        helmChart: './jjwebapicore/charts/jjtest/'
        helm-version: 'latest'
        silent: 'false'
      id: bake-test
